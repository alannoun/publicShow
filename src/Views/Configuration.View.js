import React from "react";
import { Configuration } from "../components/Configuration/components/Configuration.component";

export const ConfigurationView = ({ navigation }) => {
  return <Configuration navigation={navigation} />;
};
