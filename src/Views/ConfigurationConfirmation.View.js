import React from "react";
import { ConfigurationConfirmation } from "../components/Configuration/components/ConfigurationConfirmation.component"

export const ConfigurationConfirmationView = ({ navigation, route }) => {
  return <ConfigurationConfirmation navigation={navigation} route={route} />;
};
