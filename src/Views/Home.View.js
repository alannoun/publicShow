import React, { useContext } from "react";
import { Text } from "react-native";
//import styled from "styled-components/native";
import { HomeDetail } from "../components/Home/components/HomeDetail.Component";

export const HomeView = ({ route }) => {
  return <HomeDetail />;
};
